import { CountColorPipe } from './count-color.pipe';

describe('CountColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CountColorPipe();
    expect(pipe).toBeTruthy();
  });
});
