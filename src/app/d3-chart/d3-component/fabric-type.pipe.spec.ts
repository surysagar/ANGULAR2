import { FabricTypePipe } from './fabric-type.pipe';

describe('FabricTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FabricTypePipe();
    expect(pipe).toBeTruthy();
  });
});
